git secrets --install

# Register AWS secret scanner
git secrets --register-aws

#List the git secrets regular expressions that are searching the repository
git secrets --list

# Scan current directory recursively
git secrets --scan

